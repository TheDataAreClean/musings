---
layout: layouts/base.njk
title: Shots
---

<div class="doc-page">

  <p class="doc-title">Shots</p>

  <p class="doc-nav">
    <a href="/">Home</a> · <a href="/ideas/">Ideas</a> · <a href="/notes/">Notes</a> · <a href="/shots/">Shots</a> · <a href="/about/">About</a>
  </p>

  <p class="doc-meta">{{ collections.shots | length }} shots</p>

  {% if collections.shots.length %}
  <div class="post-list">
    {% for post in collections.shots %}
    <div class="post-list__item">
      <p class="post-list__title"><a href="{{ post.url }}">{{ post.data.title }}</a></p>
      <p class="post-list__line2">{% for tag in post.data.tags %}{% if tag != "shots" %}<a class="post-tag" href="/tags/{{ tag }}/">{{ tag }}</a>{% endif %}{% endfor %}<time datetime="{{ post.date | isoDate }}">{{ post.date | isoDate }}</time></p>
    </div>
    {% endfor %}
  </div>
  {% else %}
  <p>No shots yet.</p>
  {% endif %}

</div>
